<UserControl x:Class="la_Lliga_Premier.ControlClassificacio"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="650">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CLASSIFICACIÓ OFICIAL" FontSize="18" FontWeight="Bold" Margin="10,10,10,15" Foreground="#1E3A8A"/>

        <DataGrid x:Name="GridClassificacio" Grid.Row="1" Margin="5" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F8FAFC">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Equip" Binding="{Binding NomEquip}" Width="*"/>
                <DataGridTextColumn Header="Punts" Binding="{Binding Punts}" FontWeight="Bold" Width="80"/>
                <DataGridTextColumn Header="Gols Favor" Binding="{Binding GolsAFavor}" Width="95"/>
                <DataGridTextColumn Header="Gols Contra" Binding="{Binding GolsEnContra}" Width="95"/>
                <DataGridTextColumn Header="Dif. Gols" Binding="{Binding DiferenciaGols}" Width="95"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>